var express = require('express');
var router = express.Router();

/* GET bar listing. */
// router.get('/bar', function(req, res) {
//  // res.render('bar',{title:"mytitle"});
// });

module.exports = router;
